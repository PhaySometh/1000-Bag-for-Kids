// FontCheck component removed.
// Niradei font is bundled in `public/fonts/` and is used as the default font.
// This component was previously used to detect font installation for local setups.
export default function FontCheck() {
  return null;
}
